# 의존성
**/node_modules

# 빌드 결과물
**/dist

# 환경 변수
# .env.local, .env.production 같은 변형까지 막는다.
# .env.example은 어떤 변수가 필요한지 알려주는 문서이므로 추적한다.
**/.env
**/.env.*
!**/.env.example

# 로그
**/npm-debug.log*
**/yarn-debug.log*

# OS / 도구가 만드는 파일
**/.DS_Store
**/*.local
